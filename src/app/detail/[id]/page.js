import React from "react";
import UserDetail from "<prefix>/components/UserDetail";

function Detail({ params }) {
  return (
    <>
      <UserDetail params={params} />
    </>
  );
}

export default Detail;
